import 'package:flutter/material.dart';

class CodesSearching extends StatelessWidget {
  const CodesSearching({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Calls",
        style: TextStyle(fontSize: 20.0),
      ),
    );
  }
}
